const URL_BASE = "localhost:8080/"
let end_points = {
    users:URL_BASE + "users",
    jobs: `${URL_BASE}jobs`,
    candidates: URL_BASE + "candidates",
    applications: `${URL_BASE} applicatimons`
}
export default end_pointsnpm